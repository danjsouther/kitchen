node_modules/
dist/
.angular/
*.tsbuildinfo

# Env files (never commit secrets)
.env
.env.local
!.env.example

# Prisma generated client (regenerated via `npm run prisma:generate`)
/packages/backend/generated/

# Uploaded recipe images
/packages/backend/uploads/

# Open Food Facts dumps — `npm run off:download` puts a ~12.5 GB .jsonl.gz here.
# It is refreshed monthly from OFF and is never part of the repo or the image.
/packages/backend/data/

# mkcert-issued local dev certs (see docs/DEV-HTTPS.md) — machine-specific,
# never trusted outside this machine + whatever phones you've enrolled.
/packages/frontend/tls/

# OS / editor
.DS_Store
Thumbs.db

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Vendored skill dependencies (npm-installed on first use)
.claude/skills/*/node_modules/
